#include "main.h"

/**
 * _abs - To Write a function that computes the absolute value of an integer.
 *
 * Return: the abs of the int.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
